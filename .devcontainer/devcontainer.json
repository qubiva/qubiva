{
  "name": "Qubiva Demo",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {}
  },
  "forwardPorts": [80],
  "portsAttributes": {
    "80": {
      "label": "Qubiva",
      "onAutoForward": "notify"
    }
  },
  "postCreateCommand": "docker compose pull",
  "postStartCommand": "docker compose up -d && echo 'Waiting for Qubiva to be ready...' && until curl -sf http://localhost/api/health > /dev/null 2>&1; do sleep 2; done && echo '\n  Qubiva is ready!\n  Login: admin@qubiva.local / Demo@2026\n'",
  "waitFor": "postStartCommand"
}
